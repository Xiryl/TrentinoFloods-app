<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TrentinoFloods" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/violetblue</item>
        <item name="colorPrimaryVariant">@color/violetblue</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/violetblue</item>
        <item name="colorSecondaryVariant">@color/violetblue</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>


    <style name="StyleBtnBlue" parent="TextAppearance.AppCompat.Button">
        <item name="android:background">@drawable/btn_bg_rounded_blue</item>
        <item name="android:fontFamily">@font/roboto_bold</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:textColor">@color/blue</item>
        <item name="android:elevation">0dp</item>
    </style>

    <style name="StyleBtnRed" parent="TextAppearance.AppCompat.Button">
        <item name="android:background">@drawable/btn_bg_rounded_red</item>
        <item name="android:fontFamily">@font/roboto_bold</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:elevation">0dp</item>
    </style>
</resources>